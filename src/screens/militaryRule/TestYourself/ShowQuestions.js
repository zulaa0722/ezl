import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ShowQuestions = () => {
  return (
    <View>
      <Text>ShowQuestions</Text>
    </View>
  );
};

export default ShowQuestions;

const styles = StyleSheet.create({});
